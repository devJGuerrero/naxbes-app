import type React from "react";
import Setting from "@/app/ui/icon/setting";
import SignOut from "@/app/ui/icon/sign-out";
import Notification from "@/app/ui/icon/notification";

const Icon: React.FC = () => {
  return null;
}

Icon.displayName = "Icon";

export default Object.assign(Icon, {
  Setting, SignOut, Notification
});
